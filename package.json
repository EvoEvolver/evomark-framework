{
  "name": "evomark-framework",
  "license": "MIT",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "t-dev": "yarn em-dev test_project --templateDev true",
    "deploy-doc": "yarn em-build docs_src; yarn em-deploy docs_src"
  }
}